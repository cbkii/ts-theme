<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- DoFun's audited theme ABI intentionally targets API 26 on the Android 10 TS18 host. -->
    <issue id="ExpiredTargetSdkVersion" severity="ignore" />
    <issue id="OldTargetApi" severity="ignore" />
    <!-- compileSdk 29 is part of the audited plug-in ABI, not an unmaintained dependency choice. -->
    <issue id="GradleDependency" severity="ignore" />
    <!-- Keep the audited mdpi-v4 resource qualifier for host/package parity. -->
    <issue id="ObsoleteSdkInt" severity="ignore" />
    <!-- Separate DoFun control IDs intentionally share the same simple play/background glyph. -->
    <issue id="IconDuplicates" severity="ignore" />
</lint>
